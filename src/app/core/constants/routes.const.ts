/**
 * Route path tokens — single source of truth for routing.
 * Use these instead of string literals to avoid typos and simplify refactoring.
 */
export const ROUTES = {
  home: '',
  selector: 'selector',
} as const;
